
/**
 * Firebase Configuration
 * This file exports the initialized Firebase services used across the application.
 * Services are configured to handle null values gracefully by falling back to local simulation logic.
 */

// Initialize Firebase services as null to allow fallback behavior in services
export const auth = null;
export const storage = null;
